import { useElementSize } from "@mantine/hooks";
import { IResourceRow } from "./types";

interface Props {
    resources: IResourceRow[];
}

export default function CapacityBoard(){

    const {ref, height, width} = useElementSize();
    



}