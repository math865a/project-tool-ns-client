export type Child = JSX.Element | boolean | undefined | JSX.Element[] | null;
export interface IColor {
  name: string;
  backgroundColor: string;
  color: string;
}

export interface IPerson {
  id: string;
  name: string;
  color: string;
}
